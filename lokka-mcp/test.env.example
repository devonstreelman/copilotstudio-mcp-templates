# Example environment variables for testing
# Replace these with your actual values locally; do not commit secrets.

# Required for all modes
TENANT_ID=your-tenant-id
CLIENT_ID=your-client-id

# For client secret authentication
CLIENT_SECRET=your-client-secret

# For certificate authentication (uncomment if using)
# USE_CERTIFICATE=true
# CERTIFICATE_PATH=/app/cert.pem

# For interactive authentication (uncomment if using)
# USE_INTERACTIVE=true
# REDIRECT_URI=http://localhost:3000

# Optional settings
USE_GRAPH_BETA=true
